struct lcmt_stereo_control
{
    int64_t  timestamp;
    
    int8_t stereoOn;
    int8_t recOn;
    
    // to write to disk, set stereoOn to false and recOn to false
    // to pause the recording, set recOn to false and stereoOn to remain true
}
